name: steps examples

on:
  push:
    branches: [ "main" ]

jobs:
  marketplace:
    runs-on: ubuntu-latest
    steps:            
      - name: executa comando
        run: echo "Steps examples"
      - name: Setup Node.js environment
        uses: actions/setup-node@v2 # Atualizei para a versão 2
        with:
          node-version: 14 # Adicionei uma versão específica do Node.js
      - name: node verification
        run: node -v 

            

